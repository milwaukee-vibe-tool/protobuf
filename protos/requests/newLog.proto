syntax = "proto3";

message NewLog {
  // if "", use just prefix
  // else, use and apply prefix as `prefix_${log_id}`
  string log_id = 1;
  // if -1, use previous/default
  int32 sample_rate = 2;
}
